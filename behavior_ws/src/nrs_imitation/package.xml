<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>nrs_imitation</name>
  <version>0.1.0</version>
  <description>ROS2 node for ACT policy inference with Isaac Sim UR10e</description>

  <maintainer email="lexondms1@g.skku.edu">eunseop</maintainer>
  <license>MIT</license>

  <!-- ========== Build tool ========== -->
  <buildtool_depend>ament_python</buildtool_depend>

  <!-- ========== Runtime dependencies ========== -->
  <exec_depend>rclpy</exec_depend>
  <exec_depend>std_msgs</exec_depend>
  <exec_depend>std_srvs</exec_depend>
  <exec_depend>sensor_msgs</exec_depend>
  <exec_depend>geometry_msgs</exec_depend>
  <exec_depend>cv_bridge</exec_depend>
  <exec_depend>python3-opencv</exec_depend>
  <exec_depend>python3-h5py</exec_depend>
  <exec_depend>message_filters</exec_depend>

  <!-- Robot controller mode/PTP service (dev_ws/y2_ur10skku_control) -->
  <exec_depend>y2_rob_motion_interfaces</exec_depend>

  <!-- Stain-relative frame adapter: consumed by inference_core only when the
       checkpoint's dataset_stats.pkl has use_relative_position=True. Optional
       at import time, so this is a soft runtime dependency. -->
  <exec_depend>stain_relative_frame</exec_depend>

  <!-- Joystick -->
  <exec_depend>joy</exec_depend>

  <!-- Launch -->
  <exec_depend>launch</exec_depend>
  <exec_depend>launch_ros</exec_depend>
  <exec_depend>rqt_image_view</exec_depend>

  <!-- ========== Test dependencies ========== -->
  <test_depend>ament_copyright</test_depend>
  <test_depend>ament_flake8</test_depend>
  <test_depend>ament_pep257</test_depend>
  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
